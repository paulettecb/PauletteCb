Text input with optional label, hint, and error state. Periwinkle focus ring, generous 16px radius.

```jsx
<Input label="Email" placeholder="you@email.com" hint="We only send the good stuff." />
<Input label="Dog's name" placeholder="Daisy" error errorText="Required" />
```
