name: Deploy to Netlify

# Deploy activo del sitio personal (paulettecb.com) en Netlify.
# Requiere UN secret en este repo (Settings → Secrets and variables →
# Actions → New repository secret):
#   NETLIFY_AUTH_TOKEN — el mismo personal access token de Netlify que
#                        usas en el repo de Motion Lab (User settings →
#                        Applications → New access token)
#
# Ajusta "npm run build" y "publish-dir" si este repo usa otro
# framework/comando o carpeta de salida distinta a dist/.

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Build + deploy (paulettecb)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Verificar que el secret de Netlify esté configurado
        run: |
          if [ -z "${{ secrets.NETLIFY_AUTH_TOKEN }}" ]; then
            echo "::error::Falta el secret NETLIFY_AUTH_TOKEN (Settings → Secrets and variables → Actions). Sin él, el deploy se salta en silencio y este repo parece desplegado aunque no lo esté."
            exit 1
          fi
      - name: Deploy a Netlify
        uses: nwtgck/actions-netlify@v3
        with:
          publish-dir: './dist'
          production-branch: main
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: 'Deploy ${{ github.sha }}'
          enable-pull-request-comment: false
          enable-commit-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: 51518b92-b712-4ade-8340-d38b29689ffb
        timeout-minutes: 5
